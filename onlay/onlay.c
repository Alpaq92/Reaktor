/* onlay.c - the implementation, so onlay.h can be included anywhere.
 *
 * Onlay is a layout engine and nothing else: it computes rectangles and knows
 * nothing about widgets, drawing or events. See onlay.h and README.md. */
#define LAY_IMPLEMENTATION
#include "onlay.h"
